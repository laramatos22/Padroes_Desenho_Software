package AbstractFactory;

/**
 * Animal é uma classe abstrata com o método abstrato makeSound(). 
 * Subclasses de Animal implemetam o método makeSound().
 */

public abstract class Animal {
    public abstract String makeSound();
}
